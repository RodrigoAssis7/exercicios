import math
ang = float(input('Digite oi angulo: '))
sinn = math.sin(math.radians(ang))
coss = math.cos(math.radians(ang))
tang = math.tan(math.radians(ang))
print('Seu seno sera {:.2f}\nSeu cosseno sersa {:.2f}\nE sua tangente sera{:.2f}'.format(sinn, coss, tang, ))